# Init
type Query
type Mutation

# Scalars
scalar Date
scalar Datetime
scalar Timestamp

# Pagination
type PageInfo {
    totalCount: Int!
    next: String
    prev: String
    hasMore: Boolean!
}

input PageInput {
    size: Int
    cursor: String
}

